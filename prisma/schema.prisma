// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url = env("DATABASE_URL")
  relationMode = "prisma"
}

model CaptainPick {
  id  String @id @default(cuid())
  name String
  count Int
  playerElementId Int @unique
  chosenAsCaptainPercentage Float
  updatedAt DateTime @updatedAt
  createdAt DateTime @default(now())
}


model EffectiveOwnership {
  id  String @id @default(cuid())
  name String
  count Int
  playerElementId Int @unique
  chosenEffectiveOwnershipPercentage Float
  updatedAt DateTime @updatedAt
  createdAt DateTime @default(now())
}

model PlayerPicks {
  id  String @id @default(cuid())
  name String
  count Int
  playerElementId Int
  type String
  updatedAt DateTime @updatedAt
  createdAt DateTime @default(now())
}

model PriceChangesIncrease {
  id  String @id @default(cuid())
  playerElementId Int 
  name String
  cost String
  team String
  updatedAt DateTime @updatedAt
  createdAt DateTime @default(now())
}

model PriceChangesDecrease {
  id  String @id @default(cuid())
  playerElementId Int 
  name String
  cost String
  team String
  updatedAt DateTime @updatedAt
  createdAt DateTime @default(now())
}

model PriceChanges {
  id  String @id @default(cuid())
  playerElementId Int 
  name String
  cost String
  team String
  type String
  Date DateTime @default(now())
  updatedAt DateTime @updatedAt
  createdAt DateTime @default(now())
}

model PriceChangesGameweek {
  id  String @id @default(cuid())
  playerElementId Int 
  name String
  cost String
  team String
  type String
  gameweek Int
  Date DateTime @default(now())
  updatedAt DateTime @updatedAt
  createdAt DateTime @default(now())
}

// model CronTasks {
//   id  String @id @default(cuid())
//   scheduledExecutionTime DateTime
//   targetUrl String // domain.com/api/players/do-something
//   method String // GET | POST
//   queryParams String? // encoded query params message=hello&playerId=123
//   payload String? // Use superjson library to store arguments as a string
//   updatedAt DateTime @updatedAt
//   createdAt DateTime @default(now())
// }